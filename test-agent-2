pipeline {
    agent {label 'Agent-1'}

    stages {
        stage('Hello') {
            steps {
                git branch: 'test', credentialsId: 'Git-credentials-for-pramod', url: 'https://github.com/pramod2007m/testrepo.git'
                echo 'Hello World'
            }
        }
    }
}